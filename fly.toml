app = "github-actions"
primary_region = "sin"

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true

[[mounts]]
  source = "auth_data"
  destination = "/app/auth"
